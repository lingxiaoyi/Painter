<view>
  <view class="canvas-box">
    <painter customStyle='margin-left:40rpx' palette="{{template}}" bind:imgOK="onImgOK" />
  </view>
  <image src="{{imagePath}}" class="img-preview"></image>
  <button style='position:relative;margin-top:50rpx' bind:tap='saveImage'>保存</button>
</view>